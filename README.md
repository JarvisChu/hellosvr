# hellosvr
a project for learning k8s


## build & run local

```bash
go build .
./hellosvr -h
./hellosvr server
```

## build docker

modify variables in build.sh, then run the shell

```bash
./build.sh
```
